#include "bits/stdc++.h"
using namespace std;

using lint=int64_t;
const lint linf=1ll<<61;

struct Start{
	Start(){
		cin.tie(nullptr);
		ios::sync_with_stdio(0);
		cout<<fixed<<setprecision(10);
	}
} __start;
